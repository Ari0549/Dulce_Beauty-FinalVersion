"use strict";

var _react = _interopRequireDefault(require("react"));

var _react2 = require("@testing-library/react");

var _emailInput = _interopRequireDefault(require("../email-input"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('EmailInput component', () => {
  afterEach(() => {
    (0, _react2.cleanup)();
  });
  describe('The Rendered HTML', () => {
    it('should have a label', () => {
      var {
        getByText,
        container
      } = (0, _react2.render)(_react.default.createElement(_emailInput.default, {
        label: "My EmailInput Label"
      }));
      expect(container.firstChild).toMatchSnapshot();
      var label = getByText('My EmailInput Label');
      expect(label.textContent).toBe('My EmailInput Label');
    });
  });
  describe('handling input', () => {
    it('should set a value when passed in but not be valid', () => {
      var {
        getByLabelText,
        container
      } = (0, _react2.render)(_react.default.createElement(_emailInput.default, {
        label: "My Example Email",
        initialValue: "name@example.com"
      }));
      expect(container.firstChild).toMatchSnapshot();
      var input = getByLabelText('My Example Email');
      expect(input.value).toBe('name@example.com');
      expect(input.validity.valid).toBe(true);

      _react2.fireEvent.change(input, {
        target: {
          value: 'Updated Value'
        }
      });

      expect(input.validity.valid).toBe(false);
    });
  });
});